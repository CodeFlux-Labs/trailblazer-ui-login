import { registerRootComponent } from 'expo';
import App from './src/App'; // Adjust the path if your App component is in a different location

// Register the main application component
registerRootComponent(App);